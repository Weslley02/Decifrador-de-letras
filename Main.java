class Main {
  public static void main(String[] args) {
    int i;
    Word word = new Word();
    word.set Word("O lanche está dois reais!");
    String contrario[]=word.getWord().split(" ");
    For(i=contrario - 1;i>=0;i--){
    System.out.println(contrario[i] + contrario[i].length ());
    
    }
  }
}